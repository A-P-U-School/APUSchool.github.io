# APUSchool

## ローカルプレビュー

初回のみ依存関係をインストールします。

```sh
bundle config set path vendor/bundle
bundle install
```

プレビューサーバーを起動します。

```sh
bundle exec jekyll s
```

起動後、ブラウザで `http://localhost:4000` を開くと確認できます。
